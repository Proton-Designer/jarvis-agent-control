{
  "allowed": {
    "/compact": "none",
    "/cost": "readonly",
    "/usage": "readonly",
    "/status": "readonly"
  },
  "blocked": {
    "/config": "interactive settings picker -- confirmed live that Enter on a highlighted row toggles a real, global setting (disabled Ayman's auto-compact preference by accident during testing, 2026-08-17). Never send.",
    "/model": "falls through into the same Config picker (bare /model, no args, in this Claude Code version) -- same hazard as /config until specifically re-verified safe with an argument. Blocked wholesale, including '/model <name>' forms, until that verification happens.",
    "/help": "opens a persistent view with no input prompt, same dismissal hazard class as /config -- not yet classified readonly vs interactive, blocked pending that classification.",
    "/clear": "irreversibly wipes the conversation, unlike /compact (non-destructive, summarizes and preserves the thread). A misheard or misrouted /clear destroys accumulated context with no way to recover it. Near-zero voice-specific value -- clear is disabled for voice; run it manually. Set JARVIS_ALLOW_CLEAR=1 to enable if a stronger confirmation flow is built for it later."
  }
}
